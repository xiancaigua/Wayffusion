# BC 模范配置，推荐从 cnn_deepsets 起步
name: bc_cnn_deepsets_template
algorithm: bc
policy_class: cnn_deepsets
cnn_channels: [16, 32, 64]
agent_hidden_dim: 64
joint_hidden_dim: 256
batch_size: 64
epochs: 15
learning_rate: 0.0003
eval_interval: 2
