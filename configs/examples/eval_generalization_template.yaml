# 泛化 baseline 评估模范配置
mode: generalization
train_env_config:
  num_agents: 4
  obstacle_density: 0.08
  scaling_mode: fixed_map
test_env_config:
  num_agents: 8
  obstacle_density: 0.14
  scaling_mode: density_preserving
policies:
  - heuristic
  - random
episodes: 20
output_dir: outputs/eval/generalization
